

Дадена е следната декларация на функция:
    int test(int n, int &m);
Отбележете верните оператори, ако цялата
променлива ind има начална стойност 0.
+ cout << test(5, ind);
- cout << test(ind, 1);


Дадена е функцията:
   void odd_num(int n)
   {   if (n%3 > 1) cout << n;
       else         cout << n+1;
   }
Отбележете случаите на извикване на функцията,
при които ще се отпечата нечетно число.
+ odd_num(0);
- odd_num(1);



Отбележете оператор от главна функция main, който ще
отпечата числото 24.
   long new_fact(int n)
   {  if (n == 1) return 1;
      else
      {  long result = n * new_fact(n - 1);
         return result;   }
   }
+ cout << new_fact(4);
- cout << 3*new_fact(3);



Отбележете правилно написаните оператори на С++. Всички използвани
променливи са предварително дефинирани от тип int и имат стойност 1.
+ if (s < 10) cout << s;
- do (go > 0) {cout << go; go -= 2;}



Отбележете цикъл, който ще се изпълни точно 2 пъти. Променливата i
има начална стойност 0;
+ for(i=2; i<4; i++) cout << i;
- for(i=0; i<=2; i++) cout << i;



Отбележете циклите, които не са теоритично безкрайни.
k е променлива от тип int и има стойност -1.
+ while (k<0) k++;
- while (true) k++;



Отбележете верните твърдения за класове.
+ Всяка член-функция има точно един неявен параметър.
- Всяка член-функция има точно един явен параметър.



Даден е класът
   class Employee {
   public:
      Employee();
      Employee(string nam, double sal);
      void set_salary(double new_sal);
      string get_name() const;
      double get_salary() const;
   private:
      string name;
      double salary;  };
и обект от този клас e, дефиниран в main. Отбележете операторите,
които могат да се срещат в блока на функцията main.
+ cout << e.get_name();
- cout << e.name;



Отбележете верните твърдения за вектора v.
+ Векторът v е множество от данни от един и същи тип.
- v[0] е винаги нула.



Даден е вектор и стойности на елементите му.
   vector<double> a(2);
   a[0] = 0.5; a[1] = 1.5;
Отбележете изразите, чиято стойност е 2.
+ a.size()
- a[0]



Отбележете верните твърдения за потоци и работа с файлове.
+ За работа с файлове трябва да се включи заглавният файл fstream.
- За работа с файлове трябва да се включи заглавният файл iostream.
